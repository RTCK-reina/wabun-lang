# 和文 — 古文の文法にて算法を綴る言語

> 是 wabun-lang なり。古典日本語（文語体）の文法をそのまま構文として用ふる難解言語にして、漢文（中国古典）を構文に持つ wenyan-lang の和文版に当たる。
> 係り結び・助動詞・助詞による格表示を構文の要素に写し取り、書きたるコード即ち擬古文として読まるる事を旨とす。
> 終はりにジャヴァ筆記（JavaScript）に翻訳して走らす。

```
※ 世界に挨拶せむ ※
「世界」を書きけり。
```

```
※ FizzBuzz の事 ※
一より十五まで n を一つずつ増しつつ、
　n を 三 にて割りたる余り、零に等し、且つ n を 五 にて割りたる余り、零に等しならば、
　　「FizzBuzz」を書きけり。
　然らずして、n を 三 にて割りたる余り、零に等しならば、
　　「Fizz」を書きけり。
　然らずして、n を 五 にて割りたる余り、零に等しならば、
　　「Buzz」を書きけり。
　然らずば、
　　n を書きけり。
　然らば終はり。
繰り返し終はりぬ。
```

## 是にて何ができるか

漢数字直書、文字列直書、真偽（`真`／`偽`）、空（`無し`）。集合直書 `〔一、二、三〕` あるいは `集ひ：…、集ひ終はり`。録（連想配列）直書 `録：「鍵」は値、…、録終はり`。括弧 `（…）`。変数宣言と再代入。算術と文字列連結。比較と論理。条件分岐、範囲反復、条件反復、各々反復。業（関数）定義と再帰。例外処理（`試みに…過つに…試み終はりぬ` と `投ぐ`）。読み入れ（他文を取り込む事、循環を検めて二度入らず）。入出力。

組み込みの業、四十余り：算術、三角の関数、対数、冪、円周率、自然底、乱数、文字列、集合、高階関数（写し替へ・選び出し・束ね・巡らす・一切・孰れか）、録の鍵列挙・値列挙、日付・時刻、変換（数値化＝漢数字対応・文字列化・型）。

完全に和文化されたる誤りの報せ（曰く、`構文解析誤り：『を』を期待せしも『ならば』来たり（位置 七：廿八）` 也）。

卓上にて実行、ジャヴァ筆記に翻訳、対話処（REPL）、閲覧場（Webプレイグラウンド）、いずれも備ふ。

閲覧場は世に公けにしたれば、揃へずとも直ちに触れらる。左の所に在り：https://rtck-reina.github.io/wabun-lang/

## 揃へ方

```bash
git clone https://github.com/RTCK-reina/wabun-lang.git
cd wabun-lang
# 節点機関（Node.js）十八版以上を要す。外部ライブラリは無し。
```

## 用ひ方

### 卓上にて実行する事

```bash
node bin/wabun.js run examples/fizzbuzz.wb
```

### ジャヴァ筆記に翻訳する事

```bash
node bin/wabun.js compile examples/factorial.wb -o factorial.js
```

### 構文木を覗く事（試走の用）

```bash
node bin/wabun.js ast examples/hello.wb
```

### 対話処にて遊ぶ事

```bash
node bin/wabun.js repl
> 三を 値 と云ふ。
> 値 に （値 に 一 を加へたる）を改む。
> 値 を書きけり。
四
> やめ
```

### 閲覧場にて開く事

公けの閲覧場、揃へずとも触れらる：https://rtck-reina.github.io/wabun-lang/

手元にて動かさむとせば、左の如し。

```bash
npm run build
open playground/index.html
```

`dist/wabun.bundle.js` は `src/` より生ぜし物にして、蔵（リポジトリ）に納めたり。`src/` を改めたらば `npm run build` を行ひ、`dist/` も共に納むべし。怠らば検め（CI）に咎めらる。

備ふる物、左の如し。

- 和紙風の意匠、墨・朱・苔・紺の四色構文ハイライト（即時に染まる）
- 書き入るる毎の自動実行（三百五十粍秒の溜め）
- 結果・構文木・成果文・字句列の四面切替
- 縦書き表示の切替（古文として読み下したき折に用ふ）
- 経路（URL）への秘め込み（按鈕一つにて写し取り）
- `⌘ もしくは ^ ＋ Enter` にて即実行
- `Tab` にて全角空白挿入
- 例題十有四（基礎構文より高階関数・例外・数当て・じゃんけん・古文クイズに至るまで）

## 言葉の手触り

### 変数の宣言と算術の式

```
三を 値甲 と云ふ。
四を 値乙 と云ふ。
値甲 に 値乙 を加へたる を書きけり。   ※ 七 ※
値甲 に 値乙 を掛けたる を書きけり。   ※ 十二 ※
十 を 三 にて 割りたる余り を書きけり。 ※ 一 ※
```

| 古文の表現 | 算法の意 |
|---|---|
| `A に B を加へたる` | A 足す B |
| `A より B を引きたる` | A 引く B |
| `A に B を掛けたる` | A 掛ける B |
| `A を B にて 割りたる` | A 割る B |
| `A を B にて 割りたる余り` | A を B で割りたる剰り |
| `A に B を継ぎたる` | 文字列の連結 |

### 比較と論理

```
値、五に等し       ※ 値が五なるか ※
値、五より大なり    ※ 値が五より大きいか ※
値、五に劣らず     ※ 値が五以上なるか ※
値、五に非ず       ※ 値が五ならざるか ※
A、且つ B          ※ A も B も ※
A、或いは B         ※ A または B ※
```

### 括弧（優先順位を明示する事）

算術の優先順位は同レベル左結合なれば、優先順位を変へむと欲せば `（）` にて明示すべし。

```
（三 に 四 を加へたる）に 五 を掛けたる を書きけり。   ※ 三十五 ※
三 に （四 に 五 を掛けたる）を加へたる を書きけり。   ※ 廿三 ※
```

### 録（連想配列、または巻物に喩ふ）

```
録：「名」は「太郎」、「年」は二十、録終はり を 人 と云ふ。
人 の「名」を書きけり。   ※ 太郎 ※
人 の「年」を書きけり。   ※ 二十 ※
```

### 標準の業

```
絶対値 に 負の七 を奉りたる を書きけり。              ※ 七 ※
最大 に 〔三、一、四、一、五、九〕 を奉りたる を書きけり。 ※ 九 ※
逆順 に 「いろはにほへと」 を奉りたる を書きけり。      ※ とへほにはろい ※
結ぶ に 〔「松」、「竹」、「梅」〕 と 「・」 を奉りたる を書きけり。  ※ 松・竹・梅 ※
数値化 に 「一万二千三百四十五」 を奉りたる を書きけり。   ※ 一万二千三百四十五 ※
```

### 高階関数（業を業に渡す事）

```
n を受けて返す業を 二倍 と云ふ。
　n に 二 を掛けたる を返すなり。
業終はりぬ。

写し替へ に 〔一、二、三、四、五〕 と 二倍 を奉りたる を書きけり。
　※ 〔二、四、六、八、十〕 ※
```

### 例外の処置

```
試みに、
　「危ない事よ」を投ぐ。
過つに、e を以て、
　「捕まへたり：」 に e を継ぎたる を書きけり。
試み終はりぬ。
　※ 捕まへたり：危ない事よ ※
```

### 読み入れ（他の文を取り込む事）

```
※ examples/lib.wb なる別文に在り ※
n を受けて返す業を 二倍 と云ふ。
　n に 二 を掛けたる を返すなり。
業終はりぬ。

※ 主たる文 ※
「./lib.wb」を 読み入れけり。
二倍 に 五 を奉りたる を書きけり。   ※ 十 ※
```

### 集合・録の要素を改めたる事

```
〔一、二、三〕を 数 と云ふ。
数 の 二 番目 に 二十 を改む。
数 を書きけり。   ※ 〔一、二十、三〕 ※
数 の 一 番目 を削る。
数 を書きけり。   ※ 〔二十、三〕 ※

録：「名」は「太郎」、録終はり を 人 と云ふ。
人 の「年」に 二十 を改む。
人 を書きけり。   ※ 録：「名」は太郎、「年」は二十、録終はり ※
```

### 業（関数）定義（再帰の階乗）

```
n を受けて返す業を 階乗 と云ふ。
　n、一に勝らずならば、
　　一を返すなり。
　然らば終はり。
　階乗 に （n より 一 を引きたる）を奉りたる を 下 と云ふ。
　n に 下 を掛けたる を返すなり。
業終はりぬ。

階乗 に 五 を奉りたる を書きけり。  ※ 百二十 ※
```

呼ぶには「`f に 引数 を奉りたる`」（式として）または「`f に 引数 を奉りけり`」（文として）。

## 構成（家屋の間取りに喩へて）

```
wabun-lang/
├── src/                源流の処
│   ├── lexer.js      字句解析器（漢数字を解き、予約語の最長一致を取る役）
│   ├── parser.js     再帰下降構文解析器（係り結びと助動詞を構文木に変ふ役）
│   ├── compiler.js   構文木よりジャヴァ筆記へ翻訳する役
│   ├── runtime.js    実行時の下働き（出力・型変換・索引参照、ほか）
│   ├── builtins.js   組み込みの業（絶対値・部分・並べ替へ、ほか）
│   └── index.js      公の窓口
├── bin/
│   └── wabun.js      卓上にて呼ばるる物
├── playground/
│   └── index.html    閲覧器に開く遊び場
├── examples/         例題の所
├── docs/
│   └── SPEC.md       言語の仕様書
├── scripts/
│   └── build-playground.js  遊び場用の束ね生成器
└── test/
    └── run.js        記録試験
```

## 設計の心構へ

- **識別子は鉤括弧で囲まず。** 文字列直書との区別を明らかにする為。例へば変数の名は `年齢` にして、文字列は `「年齢」` なり。
- **演算子の優先順位は単純化す。** 算術 ＜ 比較 ＜ AND ＜ OR の階層は保てども、算術内部は同レベル左結合とす（`A に B を加へたるに C を掛けたる` は即ち `(A+B)*C` の意なり）。優先順位を変へむと欲せば `（）` にて明示すべし。
- **目的言語はジャヴァ筆記のみとす。** 配布は翻訳方式にして、`async` の用ある故、節点機関十八版以上を必す。
- **予約語辞書は最長一致を取る。** `繰り返し終はりぬ` と `終はりぬ` の如き曖昧の事は、辞書を長き順に並べ替へて解く。
- **内部識別子は `_和_` の接頭辞、ランタイムは `__和__`。** ユーザの命名と衝突せざる為に全和の接頭辞を採用せり。

## 未だ手の届かざる事

- BigInt（京以上の整数の安全なる表現）
- 生成業／yield／async 制御
- マクロ・メタプログラミング
- 標準入力以外の卓上呼び出し（ファイル読み書き、網絡）— 必要とあらばジャヴァ筆記に翻訳して後に実装すべし

## 許諾

マサチューセッツ工科大学許諾（MIT 許諾）— 二〇二六年 © RTCK
