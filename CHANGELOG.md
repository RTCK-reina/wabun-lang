# 変更履歴

## v1.4.0 — 2026-04-27

### LLM 用完全指南書を追加

`docs/llm-prompt.md`（約 500 行、自己完結）。これを Claude / GPT / Gemini 等のシステムプロンプトに丸投げするか、`和文ライブラリ準拠で書け` と指示した上で与えると、その LLM が和文を書けるようになる。

含まれる内容：
- ひと言サマリ（30 秒で全像把握）
- 絶対規則（破ると即パースエラーする 10 項目）
- リテラル早見・構文早見（コピペで使える表）
- 標準ライブラリ早見表（和語名と一般語の対応表、40 業以上）
- やってはいけない 9 項目（LLM が踏みやすい罠を先回り）
- 完全な正例 12 本（Hello World から再帰階乗・高階関数・例外・録まで）
- 影詞モード差分（vec／swizzle／専用業／禁止リスト）
- LLM への指示テンプレ（コピペ用）
- 一行プロンプト（最短指示）
- 自己確認チェックリスト

## v1.3.1 — 2026-04-27

### 影詞バグ直し
- 影詞バックエンドが Index ノード（`〜の N 番目`）を未対応で `未対応の式「索引」` を吐いていた問題を修正。`vec[N-1]` への翻訳を追加。
- 影詞バックエンドが GLSL ES 1.00 の識別子制約（ASCII のみ）を無視して CJK 識別子をそのまま出力していた問題を修正。`uniform vec2 解像度` などが WebGL のシェーダコンパイラに弾かれ、プレビューが真っ暗になっていた。
  - 組み込み uniform を `u_resolution` / `u_time` に固定
  - ユーザの和文識別子（変数・業）はコードポイントベースで ASCII エスケープ（`主` → `_w_4e3b`）
  - プレイグラウンドの `getUniformLocation` も追従
- プレイグラウンドの WebGL canvas が flex レイアウトで高さ 0 になっていた問題を修正（`flex:1; min-height:0` 追加）
- 影詞コンパイル失敗時に出力欄が canvas のままで誤りが見えなかった問題を修正（失敗時は出力欄に切替＋結果札にフォーカス）

### プレイグラウンド UI のカタカナ語狩り
ユーザに見える文字列・コメントから可能な限りカタカナを排除：

| 旧（カタカナ） | 新（和語） |
|---|---|
| プレイグラウンド | 遊び場 |
| プログラミング | 算法綴り |
| プレビュー | 下覧 |
| プロンプト | 問ひ |
| ハイライト | 彩色 |
| シンタックス | 構文 |
| シンタックスハイライト | 構文彩色 |
| ボタン | 按鈕 |
| トースト | 知らせ |
| ライブ | 即時 |
| シェア | 共有 |
| リンク | 紐帯 |
| エディタ | 編集場 |
| イベント | 事象 |
| モード | 態 |
| コメント | 注釈 |
| コンパイル | 翻訳 |
| コード | 綴り |
| コードポイント | 字号 |
| グラデーション | ぼかし |
| ノイズ | 紋様 |
| ハッシュ | 位置情報 |
| スキップ | 省略 |
| スペース | 空白 |
| ミラー | 鏡映 |
| クラス | 階級 |
| ラベル | 札 |
| パース | 構文解析 |
| プレーン | 素のまま |
| キーワード | 予約語 |
| クイズ | 問答 |
| レコード | 録 |

例題の鍵名も和文化：`hello → 挨拶`、`arithmetic → 算術`、`conditional → 条件分岐`、`array → 集合`、`record → 録`、`highorder → 高階関数`、`try → 試捕`、`mutate → 要素改め`、`guess → 数当て`、`janken → じゃんけん`、`quiz → 歌問答`。

許容して残置：`グー / チョキ / パー`（じゃんけんのゲーム文字列）、`フィボナッチ`（数学者の固有名詞）、`FizzBuzz`（プログラミング教育の固有題名）。

## v1.3.0 — 2026-04-27

### 影詞（かげことば）— GLSL fragment shader バックエンド追加

和文 (wabun-lang) のサブセット＋ベクトル拡張で fragment shader を記述できる新バックエンド。Shader = to shade（陰影をつける）の直訳から命名。

- ファイル拡張子 `.kg`、サブコマンド `wabun compile-影詞` （`compile-kageshi` も可）
- 同じ字句解析・構文解析を共有、出力先のみ JS から GLSL ES 1.00 に切替
- 組み込み入力 uniform：`uv`（vec2、画素正規化座標）／`時`（float、経過秒）／`解像度`（vec2）
- 組み込み出力：業 `主` の戻り値 vec4 → `gl_FragColor`
- ベクトル型：`〔a、b〕` → vec2、`〔a、b、c〕` → vec3、`〔a、b、c、d〕` → vec4
- swizzling：`v の「赤」` → `v.r`、`v の「横」` → `v.x` など8種
- 数値リテラル → float（自動で `.0` 付与）
- 影詞専用業：`混ぜ` `端` `滑らか段` `分数` `歩` `長さ` `正規化` `内積` `外積` `距離` `反射`
- 既存業の流用：`絶対値` `平方根` `切り捨て` `切り上げ` `最大` `最小` `冪` `自然対数` `正弦` `余弦` `正接`
- 数学定数：`円周率` `自然底`
- 範囲反復は数値リテラル上下限のみ許可
- 副作用・入出力・例外・録・読み入れは禁止（GLSL に対応物なし）
- プレイグラウンドに**影詞モード切替按鈕**と**WebGL リアルタイムプレビュー**を追加
- 例題3本：`虹（影詞）` `波（影詞）` `円（影詞）`

### パーサ改善
- `IDENT に … を 奉りたる` の関数呼び出し判定をバックトラック方式に切替（`d に 二十 を 掛けたる` のような演算式と曖昧性なく解決）
- parseArith のループで `を 奉りたる/奉りけり` を演算終端として認識（Call の閉じを優先）

## v1.2.0 — 2026-04-27

### 完全和文化

ユーザの目に触れるあらゆる英語要素を可能な限り和文化。
- 遊び場の按鈕・札（ラベル）：「AST→構文木」「JS→成果文」「トークン→字句」「URL→経路」
- 行・列表示を漢数字（`七行廿八列`）
- 字数表示を漢数字（`百二十三字`）
- トークン型名 を全部漢字へ：`NUM→数値` `STRING→文字列` `IDENT→識別子` `KW→予約` `DOT→句点` `COMMA→読点` `ARRAY_OPEN→集開` `ARRAY_CLOSE→集閉` `PAREN_OPEN→括開` `PAREN_CLOSE→括閉` `COLON→二点` `EOF→文末`
- 構文木ノード名 を全部漢字へ：`Program→全体` `VarDecl→変数宣言` `Print→出力` `If→条件` `RangeLoop→範囲反復` `WhileLoop→条件反復` `ForEach→各々反復` `FuncDecl→業宣言` `Return→返却` `CallStmt→呼文` `ArrayPush→末尾加` `IndexAssign→索引代入` `MemberAssign→鍵代入` `IndexDelete→索引削` `MemberDelete→鍵削` `Throw→投擲` `TryCatch→試捕` `Import→読入` `NumLit→数値直` `StrLit→文字列直` `BoolLit→真偽直` `NullLit→空直` `Var→変数` `ArrayLit→集合直` `RecordLit→録直` `BinOp→二項` `UnaryOp→単項` `Call→呼出` `Index→索引` `Member→鍵` `Length→長`
- 内部接頭辞 `_w_` を `_和_` に
- ランタイム名 `__wabun__` を `__和__` に
- README/CHANGELOG/SPEC の英略語を漢語へ：`AST→構文木` `JS→成果文` `REPL→対話処` `CLI→卓上` `URL→経路` `transpiler→変換器` `compiler→翻訳器` `parser→構文解析器` `lexer→字句解析器`

JS 予約語（`async`/`if`/`return`/`for`/`try`/`throw`/`catch`/`let`）と固有名詞（MIT、Node.js、JavaScript、GitHub）のみ残置。これらは目的言語が JS である以上、生成成果文に必須。

## v1.1.0 — 2026-04-27

### 遊び場全面刷新
- 和紙風意匠（生成り背景・墨色・朱の差し色・SVGノイズ模様）
- 四色構文ハイライト（予約語=朱・文字列=苔・数値=紺・注釈=薄墨）を即時反映
- 入力中の自動実行（350ミリ秒の溜め）
- 結果／構文木／成果文／字句列の四面切替
- 縦書き表示の切替
- コードを含む経路での共有（`#c=...` 位置情報＋按鈕一つで写し取り）
- `Tab` で全角空白挿入（字下げ用）
- 状態欄（実行状態・行/列・字数）

### 新例題
- 数当て遊び（`guess.wb`）
- じゃんけん（`janken.wb`）
- 古文クイズ（`quiz.wb`）

### 直し
- 構文解析器の `scanForKW` が括弧範囲を正しく扱わない問題を修正（括弧の外に出たら検索打ち切り）

### 改善
- `parse-only` 試験を test/run.js に追加（入力を要する例題も構文確認のみ可能に）

## v1.0.0 — 2026-04-27

### 新機能
- 括弧 `（…）` `(…)` で式の優先順位を明示できる
- 録（連想配列）直書 `録：「鍵」は値、…、録終はり`、`obj の「鍵」` で参照
- 集合要素更新 `array の n 番目 に 値 を改む` と削除 `array の n 番目 を削る`
- 録の鍵再代入 `record の「鍵」に 値 を改む` と削除 `record の「鍵」を削る`
- 例外処理 `試みに、… 過つに、e を以て、… 試み終はりぬ` と `〈式〉を投ぐ`
- 読み入れ機構 `「path」を読み入れけり` で別ファイルを直書展開（循環検出付き）
- 漢数字を兆・京まで拡張
- 対話処 `wabun repl`
- エラー位置のソース脈絡表示（行番号と矢印）
- 標準ライブラリ大幅拡充（40+の業：算術・統計・三角関数・対数・乱数・高階関数・文字列・配列・録・日付）
- 完全和文化された誤りメッセージ

### 改善
- `Var` 参照で組み込み業を識別子としても扱える（高階関数への引数渡しが可能に）
- 束ね（バンドラ）に `builtins.js` を追加して遊び場でも全機能が動作

### 範囲確定（v1.0.0で意図的に未対応）
- 生成業／yield
- ネイティブな並行処理
- マクロ／メタプログラミング
- BigInt（京の精度限界以降）

## v0.2.0

- 括弧、録型、標準ライブラリ初版、誤りメッセージ和文化

## v0.1.0

- 言語の核（直書、変数、算術、比較、論理、条件分岐、反復、業、配列、入出力）
- 卓上、JS翻訳器、閲覧場
- 仕様書、例題、記録試験
